before_install:
  - yes | sdkmanager --install "build-tools;28.0.3" "tools" "platform-tools" "platforms;android-28" || true
  - ./gradlew androidDependencies app:androidDependencies app:dependencies backend:dependencies dependencies
